import React from "react";

import SingleLink from "./SingleLink";

const ListOfLinks = () => {
  return(
    <section className="py-6 w-[90%] mx-auto flex flex-col justify-center items-center">
      <SingleLink />
    </section>
  )
};

export default ListOfLinks;